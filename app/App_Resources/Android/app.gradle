// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.renzedijkstra.jsc"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
